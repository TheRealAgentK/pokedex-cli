name: pokedex
description: A Pokémon CLI tool — demo for CodeCamp Wellington 2026
version: 0.1.0

executables:
  pokedex: 'pokedex'

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.6.0
  http: ^1.6.0

dev_dependencies:
  lints: ">=4.0.0 <7.0.0"
  test: ^1.28.0
  mockito: ^5.6.1
  build_runner: ^2.10.4
